{{page-title 'EmfTest'}}

{{#each @model as |person|}}
  {{person.name.first}}
  {{person.name.last}}<br />
  {{#each person.addresses as |address|}}
    {{address.street}}
    {{address.city}}
    {{address.region}}
    {{address.country}}
  {{/each}}
  <br />
  {{#each person.titles as |title|}}
    {{title}}<br />
  {{/each}}

{{/each}}